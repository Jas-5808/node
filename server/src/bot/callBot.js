const { callAdminId } = require('../utils/config');

const setupCallBot = (bot) => {
    // Здесь можно добавить дополнительные команды для администратора звонков, если нужно
};

module.exports = { setupCallBot };